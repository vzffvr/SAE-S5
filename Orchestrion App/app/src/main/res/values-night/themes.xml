<resources>
    <!-- Base application theme. -->
    <style name="Theme.Orchestrion_night" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="backgroundColor">@color/black</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.Orchestrion_night</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/animated_logo_dark</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/cy_uni_branding</item>
    </style>

    <style name="Theme.Piano" parent="Theme.AppCompat.NoActionBar">
        <item name="backgroundColor">@color/black</item>
    </style>

    <style name="KeyButtonStyle" parent="android:Widget.Button">
        <item name="android:layout_width">74dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginStart">2dp</item>
        <item name="android:gravity">bottom|center_horizontal</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="SharpkeyButtonStyle" parent="android:Widget.Button">
        <item name="android:layout_width">52dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@drawable/black_key_background</item>
        <item name="android:layout_marginTop">-25dp</item>
    </style>
</resources>